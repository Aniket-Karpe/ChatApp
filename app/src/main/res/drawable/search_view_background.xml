<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" />
    <stroke
        android:width="2dp"
        android:color="@android:color/black" />
    <!-- Set a non-zero radius for rounded corners -->
    <corners android:radius="8dp" /> <!-- Adjust the value to control the roundness -->
    <padding
        android:left="4dp"
        android:top="4dp"
        android:right="4dp"
        android:bottom="4dp" />
</shape>
